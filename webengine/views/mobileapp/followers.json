{
  "data" :
  [
      {{each users as member where member.followed_users LIKE '%{get_var.zuid}%'  }}
      {{member.toJSON(2)}}
      {{member._arraycomma}}
      {{/each}}
  ],
  "meta" : {
    "zuid" : "{{get_var.zuid}}"
  }
}